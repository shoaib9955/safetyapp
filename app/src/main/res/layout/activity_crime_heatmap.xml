<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_crime_heatmap"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".CrimeHeatmapActivity">

    <!-- Google Map Fragment -->
    <fragment
        android:id="@+id/mapFragment"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Top controls container -->
    <LinearLayout
        android:id="@+id/topControls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp"
        android:background="#CCFFFFFF"
        android:elevation="4dp">

        <!-- Emergency number display (always visible) -->
        <TextView
            android:id="@+id/tvEmergencyNumber"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="SMS will be sent to: Not set"
            android:textSize="16sp"
            android:textColor="#000000"
            android:padding="8dp"
            android:background="@drawable/rounded_bg"
            android:gravity="center_vertical|start"/>

        <!-- Dropdown button -->
        <Button
            android:id="@+id/btnToggleDropdown"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Show Controls ▼"
            android:layout_gravity="center_horizontal"
            android:background="@drawable/rounded_button"
            android:padding="8dp"
            android:layout_marginTop="8dp"/>

        <!-- Dropdown content (initially hidden) -->
        <LinearLayout
            android:id="@+id/dropdownContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone"
            android:paddingTop="8dp">

            <!-- Map & satellite buttons -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:weightSum="2"
                android:gravity="center"
                android:layout_marginBottom="6dp">

                <Button
                    android:id="@+id/btnToggleMap"
                    android:layout_width="0dp"
                    android:layout_weight="1"
                    android:layout_height="wrap_content"
                    android:text="Toggle Zones"
                    android:background="@drawable/rounded_button"
                    android:padding="8dp"/>

                <Button
                    android:id="@+id/btnToggleSatellite"
                    android:layout_width="0dp"
                    android:layout_weight="1"
                    android:layout_height="wrap_content"
                    android:text="Satellite View"
                    android:background="@drawable/rounded_button"
                    android:padding="8dp"
                    android:layout_marginStart="8dp"/>
            </LinearLayout>

            <!-- Set number / toggle buttons -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center"
                android:weightSum="3">

                <Button
                    android:id="@+id/btnSetNumber"
                    android:layout_width="0dp"
                    android:layout_weight="1"
                    android:layout_height="wrap_content"
                    android:text="Set Number"
                    android:background="@drawable/rounded_button"
                    android:padding="8dp"/>

                <Button
                    android:id="@+id/btnToggleSMS"
                    android:layout_width="0dp"
                    android:layout_weight="1"
                    android:layout_height="wrap_content"
                    android:text="SMS: ON"
                    android:background="@drawable/rounded_button"
                    android:padding="8dp"
                    android:layout_marginStart="8dp"/>

                <Button
                    android:id="@+id/btnToggleVoice"
                    android:layout_width="0dp"
                    android:layout_weight="1"
                    android:layout_height="wrap_content"
                    android:text="Voice: ON"
                    android:background="@drawable/rounded_button"
                    android:padding="8dp"
                    android:layout_marginStart="8dp"/>
            </LinearLayout>
        </LinearLayout>
    </LinearLayout>
</RelativeLayout>
